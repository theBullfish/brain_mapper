{
  "name": "@bear-poking/api",
  "private": true,
  "scripts": {
    "dev": "uvicorn main:app --reload --port 8000",
    "start": "uvicorn main:app --port 8000"
  },
  "dependencies": {
    "fastapi": "^0.111.0",
    "uvicorn": "0.29.0"
  }
}